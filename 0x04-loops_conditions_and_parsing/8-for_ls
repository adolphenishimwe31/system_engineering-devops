#!/usr/bin/env bash
# Display files in current directory, showing part after first dash if any

for item in *; do
  if [[ -f "$item" || -d "$item" ]]; then
    if [[ "$item" == *-* ]]; then
      echo "${item#*-}"
    else
      echo "$item"
    fi
  fi
done
