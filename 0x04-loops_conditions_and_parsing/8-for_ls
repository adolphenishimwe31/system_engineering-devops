#!/usr/bin/env bash
# This script lists the contents of the current directory, showing only the part after the first dash

for item in *; do
  if [[ "$item" == *-* ]]; then
    echo "${item#*-}"
  fi
done
